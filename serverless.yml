service: workshop-${self:custom.name}

custom:
  name: enrique

provider:
  name: aws
  runtime: nodejs12.x
  region: ap-southeast-2

functions:
  get-index:
    handler: functions/get-index.handler
    events:
      - http:
          path: /
          method: get
